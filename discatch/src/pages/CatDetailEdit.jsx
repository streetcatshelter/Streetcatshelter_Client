// library
import React from 'react';

// element

// component
import { Template } from '../components';

const CatDetailEdit = (props) => {
  return <Template props={props}></Template>;
};

export default CatDetailEdit;
