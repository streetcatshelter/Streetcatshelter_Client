// LIBRARY
import React from "react";

// COMPONENTS
import { CommentList, Template } from "../components";

const test = () => {
  return (
    <Template>
      <CommentList />
    </Template>
  );
};

export default test;
